[project]
name = "bse-special-situations"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["requests==2.32.4", "pypdf==6.7.5", "python-dotenv==1.1.1", "cryptography>=44,<47", "jsonschema>=4.23,<5"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["live: explicitly enabled live API tests"]

[tool.ruff]
line-length = 110
